{
  "name": "heatmaps",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "repository": "github:flamboh/heatmaps",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint .",
    "test": "vitest run",
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check ."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "oxlint": "latest",
    "prettier": "latest",
    "typescript": "latest",
    "vitest": "latest",
    "wrangler": "latest"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "bun@1.3.10",
  "dependencies": {
    "@cf-wasm/resvg": "^0.3.5",
    "@fontsource/inter": "^5.2.8"
  }
}
